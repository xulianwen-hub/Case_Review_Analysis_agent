检索这一块：要针对不同的数据库采取不同的检索策略。等后续框架搭建完成，搜集到更多的数据后进行重构：
“针对多数据源的检索，我的核心原则是 ‘分治召回，加权融合，统一精排’ 。

在检索层，四个库采用差异化的策略——法条库强化BM25精确匹配，案例库侧重语义泛化，裁决库依赖元数据预过滤，地方通告库强制地理路由。它们在LangGraph中以并行节点形式同时发起。

在粗排层，我通过分组加权RRF来归一化四路的异构分数。前置路由节点判定的意图会直接影响各数据源的权重系数，确保‘问法条则法条优先，问当地则通告优先’。

在精排层，Cross-Encoder对所有候选文档统一打分，但我会在输入时注入文档来源标签，让模型学会区分不同来源的语言风格。如果某一路检索结果置信度过低，我会在粗排阶段直接将其丢弃，并在Prompt中显式告知LLM‘该来源无相关信息’，防止模型编造。”

“这套架构既保留了各数据源的领域特性，又保证了整个系统的简洁可控——差异在策略，统一在管道，安全的底线在于显式的空结果传递。“